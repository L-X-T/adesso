import { CityValidatorDirective } from './city-validator.directive';

describe('CityValidatorDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CityValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
